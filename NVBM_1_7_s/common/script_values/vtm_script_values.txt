

state_region_num_provinces_half = {
	value = {
		every_state = {
			limit = {
				state_region = root.state_region
			}
			add = num_provinces
		}
		multiply = 0.5
		round = yes
	}
}

state_region_num_provinces_fifth = {
	value = {
		every_state = {
			limit = {
				state_region = root.state_region
			}
			add = num_provinces
		}
		multiply = 0.3
		round = yes
	}
}
